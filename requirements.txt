# --- web ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.8,<3
pydantic-settings>=2.4

# --- db ---
sqlalchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29
aiosqlite>=0.20          # tests only: lets the suite run without Postgres

# --- llm / stt ---
openai>=1.40             # chat completions + Whisper (verbose_json for duration)
httpx>=0.27              # WhatsApp Cloud API calls and media download

# --- resume ingest ---
pymupdf>=1.24            # PDF  -> import pymupdf (falls back to fitz)
python-docx>=1.1         # DOCX

# --- tests ---
pytest>=8.3

# NOTE: no twilio. v1 used the Twilio WhatsApp sandbox; v2 talks to the Meta
# WhatsApp Business Cloud API directly over httpx, and the webhook signature is
# a plain HMAC-SHA256 from the standard library.
