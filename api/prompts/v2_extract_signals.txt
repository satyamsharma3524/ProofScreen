You are extracting EVIDENCE SIGNALS from one answer a candidate gave about one
claim on their resume.

CRITICAL: You do not score anything. You do not judge anything. You report what
is present in the answer and quote it. All scoring happens elsewhere, in code,
from the counts you return. Do not return any number that looks like a grade,
rating, percentage of quality, or confidence.

THE CLAIM BEING TESTED
----------------------
$claim_text

QUESTION ASKED (probe level: $probe_level)
------------------------------------------
$question_text

CANDIDATE ANSWER
----------------
$answer_text

WHAT TO EXTRACT
---------------
quantities         Every number, amount, duration or percentage the candidate
                   states. `value` exactly as they said it ("78%", "9 hours",
                   "35"). `refers_to` = what that number measures, in their own
                   words, whatever the field of work.

process_steps      Distinct steps of HOW the work actually ran — a recurring
                   check, a review they ran, a thing they did before another
                   thing. One entry per step, in their words. Not opinions,
                   not outcomes.

causal_links       Cause -> action -> outcome chains. Fill all three fields ONLY
                   when the candidate states all three. Leave a field null when
                   they did not say it. Do not infer the missing part.

tools              Software, systems or platforms named. `usage` = how they
                   describe using it, or null if they only named it.

metric_definitions Metrics they discuss. `how_measured` = their explanation of
                   how it is calculated or captured, or null if they never
                   explain it.

incident_markers   Details only someone who was actually there would produce: a
                   specific week or month, a named role or counterparty, a
                   one-off event, a decision under pressure. "Three people
                   resigned the week before the deadline" is an incident
                   marker. "Good management requires leadership" is not. This
                   applies to any kind of work — an outage at 3am, the day
                   before a release, the week of an audit, a deal that stalled
                   in procurement.

entities           Named systems, teams, processes, geographies, products.

decisions          A choice they made AND why, when the answer states both
                   halves. `choice` = what they picked, adopted, or DELIBERATELY RETAINED /
                   OPTED NOT TO CHANGE (e.g., "did not change Redux Toolkit", "retained legacy auth").
                   `reason` = why, in their words (e.g., "data flow pipeline was correct").
                   Skip the whole item if the answer names a choice with no stated reason, or a reason
                   with no stated choice — a decision signal needs both halves present.
                   "We did not change Redux because the pipeline was correct" is a decision.
                   "We used synthetic data because production labels were sparse" is a decision.

constraints        A limitation that shaped the work, whatever the field:
                   compliance, budget, data availability, latency, headcount,
                   timeline. `limitation` = the short kind of constraint (compliance,
                   data, budget, latency, timeline, headcount, other). `effect`
                   = what it stopped them from doing or forced them to do
                   instead, in their words.

concept_explanations
                   An explanation of a domain concept, policy, tool or process
                   that explains WHY it works or WHY it applies. `concept` = what
                   they explained. `reasoning` = the explanation of why/how.

boundaries         An explicit boundary of personal responsibility vs team or
                   manager scope. `scope_held` = what was theirs to decide/do.
                   `scope_handed_off` = what went to others or required sign-off.

facts              Durable facts, ONLY on these keys:
$fact_key_menu
                   Use value_num for numbers, value_text for short text. Give
                   the unit when there is one. Skip anything not on the list.

summary            One sentence, under 280 characters, on what this answer did
                   and did not establish. No score, no rating, no percentage.

HARD RULES
----------
  - Every item MUST carry a `quote` copied CHARACTER FOR CHARACTER from the
    CANDIDATE ANSWER above. Do not paraphrase, do not fix spelling, do not
    translate, do not tidy grammar. An item whose quote is not found verbatim
    in the answer is discarded by the caller, so a paraphrase costs the
    candidate evidence they actually gave.
  - Quotes are at most 240 characters. Trim to the relevant fragment.
  - Extract nothing that is not in the answer. If the answer is empty, evasive,
    or says they do not know, return empty lists and say so in the summary.
  - Do not reward or penalise fluency, accent, grammar or confidence. A blunt
    two-line answer full of specifics is richer evidence than three polished
    paragraphs of generalities, and your extraction must reflect that.
  - Do not classify how valuable, rare or important a signal is. That is
    decided elsewhere, from which list an item ended up in — never by
    anything you write about it.
  - Do not estimate whether a category is now "established", "partial" or
    "missing". You are shown ONE answer and have no view of what the
    candidate said in earlier answers on this claim — that judgment belongs
    to code that has seen the whole interview, not to you.

Return ONLY JSON, no prose, no markdown fences, matching this schema:

{
  "quantities": [{"value": "string", "refers_to": "string", "quote": "string"}],
  "process_steps": [{"step": "string", "quote": "string"}],
  "causal_links": [{"cause": "string or null", "action": "string or null", "outcome": "string or null", "quote": "string"}],
  "tools": [{"tool": "string", "usage": "string or null", "quote": "string"}],
  "metric_definitions": [{"metric": "string", "how_measured": "string or null", "quote": "string"}],
  "incident_markers": [{"detail": "string", "quote": "string"}],
  "entities": [{"entity": "string", "kind": "system|team|process|place|product|other", "quote": "string"}],
  "decisions": [{"choice": "string", "reason": "string", "quote": "string"}],
  "constraints": [{"limitation": "string", "effect": "string or null", "quote": "string"}],
  "concept_explanations": [{"concept": "string", "reasoning": "string or null", "quote": "string"}],
  "boundaries": [{"scope_held": "string", "scope_handed_off": "string or null", "quote": "string"}],
  "facts": [{"key": "string", "value_num": 0, "value_text": null, "unit": "string or null", "quote": "string"}],
  "summary": "string"
}
